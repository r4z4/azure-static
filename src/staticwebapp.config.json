{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["*.{css,scss,js,png,gif,ico,jpg,svg}"]
  },
  "routes": [
    {
        "route": "/catalog",
        "redirect": "/articles",
        "statusCode": 301
    }
  ]
}